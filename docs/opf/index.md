# Prim SDKs

[`product.json`](product.json) is the canonical OPF v1 graph for the Prim
SDK split. This page is a readable projection only.

**One category kernel. Each Prim type has its own internal SDK. Neither
reimplements the other.**

Daniel accepted that on 2026-08-19. The decision, the problems it answers, and
the work it still leaves are in the graph.

```bash
PYTHONPATH=~/repos-eidos-agi/opf python3 -m opf.validate ~/repos-eidos-agi/prim/docs/opf
```
